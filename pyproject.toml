[project]
name = "naturalizz"
version = "1.0.0"
authors = [
  { name="Gotye"},
]
description = "Naturalist Quizz"
readme = "README.md"
requires-python = ">=3.12"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"